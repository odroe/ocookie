name: ocookie
description: Cookie and Set-Cookie parser and serializer.
version: 0.0.0
repository: https://github.com/odroe/ocookie

environment:
  sdk: ^3.6.0

dependencies:
  http_parser: ^4.1.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
